spring:
  datasource:
    url: jdbc:tc:postgresql:15:///aerodag
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    hibernate:
      ddl-auto: create-drop
  ai:
    anthropic:
      api-key: test-key
